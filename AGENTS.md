# pick.acttub.com — 명대사 월드컵

연기해보고 싶은 장면만 남기는 16강 토너먼트. acttub 유입용 서브프로젝트.

**서버가 없다. 런타임 LLM이 없다.** 정적 파일 한 벌이고, 상태는 전부 브라우저 안에 있다.

## 이 저장소를 고칠 때

- **사용자에게 보이는 문장은 `copy.js`에만 있다.** 다른 파일에 한국어 문장을 쓰지 않는다.
  문안 정본은 내부 카피 문서이고, `copy.js`는 그것을 옮긴 것이다. 앱에서 문장을 고치면 두 곳이 갈라진다.
- **문구를 고쳤으면 `build-fonts.py`를 다시 돌린다.** 안 돌리면 새 글자가 서브셋에 없어 시스템 폰트로 렌더된다.
- **스타일을 고쳤으면 `npm run css`를 돌린다.** `styles.css`는 생성물이라 직접 고치지 않는다.
- 색·모서리·글자 크기·티어 경계는 `src/brand/`의 토큰만 쓴다. 새 값을 지어내지 않는다.
  **`src/brand/` 안의 세 파일은 브랜드 정본에서 생성된 사본이라 직접 고치지 않는다** —
  값을 바꾸려면 정본을 고치고 `tools/subproject-sync-brand.py subpro/pick --write`로 다시 받는다.
- 화면 폭·좌우 여백·safe-area·44px 탭 타겟은 `.tier-root`/`.tier-shell`이 처리한다.
  폰만 보고 `max-w-[480px]` 같은 값을 새로 박지 않는다 — 데스크톱에서 좁은 기둥이 된다.

## 판정을 하지 않는다 — 이 서비스의 존재 조건

결과 화면은 **선택을 되비추고 질문을 건넬 뿐** 사용자를 서술하지 않는다.

- 점수·등급·유형·성향·어울리는 역할·잘한다/못한다를 쓰지 않는다.
- 되비춤은 **장면**을 서술한다. 사람을 서술하면 유형 진단이 된다.
- 질문은 **극 중 인물**에 대해 묻는다. 사용자 자신에 대해 묻지 않는다.
- `data.js`의 `tag`는 결과 분기용 내부값이다. 그 값도, 그 한국어 뜻도 화면에 노출하지 않는다.

## 배포

- Vercel git 연동. **`main`에 푸시하면 곧바로 실서비스가 바뀐다.**
- 빌드 명령이 없다 — `styles.css`와 `fonts/`는 커밋된 생성물이다. 빌드를 잊으면 옛 스타일이 배포된다.
- 경로 `/r16` `/r8` `/r4` `/final` `/result/*` `/with/*`는 `vercel.json`에서 `index.html`로 넘긴다.
  **catch-all 폴백을 쓰지 않는다** — 그러면 `/_vercel/insights/*`까지 삼켜 방문 계측이 조용히 죽는다.

## 공개 저장소다

비밀값·실명·내부 문서 경로·내부 티켓 링크를 커밋하지 않는다.
